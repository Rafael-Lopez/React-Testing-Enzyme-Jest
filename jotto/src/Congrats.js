import React from 'react';

/**
 * Functional React component for congratulatory message
 * @function
 * @returns {JSX.Element} - Render component (or null if 'success' prop is passed)
 */
export default () => {
  return (
    <div></div>
  );
}
